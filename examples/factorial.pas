program factorial;
begin
    a := (5 > 10) = true and b;
    b := a;
    c := some_fn('text' = 'other_text');
    for a := 0 to 35 do
        b := 10;
    if a > b then
        a := b
    else if a < b then
        a(b)
    else
        b := a;
    while a <> b do
        begin
            a := true;
            b := false;
        end;
    a := not b[1];
end.