[package]
name = "pascal-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
err_code = "0.1.0"
itertools = "0.14.0"

[dev-dependencies]
criterion = { version = "0.8.1", features = ["html_reports"] }
test-strategy = "0.4.4"
proptest = "1.10.0"

[[bench]]
name = "my_benchmark"
harness = false
